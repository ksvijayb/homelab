---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: bookstack
spec:
  chart:
    spec:
      chart: ./charts/apps/bookstack/0
      sourceRef:
        kind: GitRepository
        name: lab-apps-public
        namespace: flux-system
  interval: 10m0s
  releaseName: bookstack
  values:
    # Image Registry
    
