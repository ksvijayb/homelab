---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plex
spec:
  chart:
    spec:
      chart: ./charts/media/plex
      sourceRef:
        kind: GitRepository
        name: lab-apps-public
        namespace: flux-system
  interval: 10m0s
  releaseName: plex
  values:
    # Image Registry
    
